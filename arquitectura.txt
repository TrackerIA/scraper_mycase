webscraper/
│
├─ config/
│   └─ settings.py           # Configuración general (EMAIL_TO_CHROME_PROFILE, rutas, etc.)
│
├─ data_layer/
│   └─ selenium_driver.py    # Inicializa y configura el driver de Selenium
│
├─ parsing_layer/
│   └─ mycase_parser.py      # Futuro: extracción de datos tras el login
│
├─ service_layer/
│   └─ mycase_service.py     # Lógica de login + navegación
│
├─ presentation_layer/
│   └─ save_results.py       # Futuro: exportar datos
│
└─ main.py                   # Punto de entrada
