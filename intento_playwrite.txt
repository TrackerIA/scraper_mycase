¡Excelente decisión! **Playwright con contexto persistente (opción C)** es, hoy por hoy, la forma más robusta para automatizar sitios que requieren **login con 2FA** porque:

* guarda las **cookies y tokens** de tu sesión en un directorio local,
* permite abrir siempre el **mismo “perfil”** (igual que un perfil de Chrome),
* y evita pelear con puertos de depuración o con los límites de Selenium.

Aquí tienes una explicación **a fondo** de cómo funciona y cómo usarlo en tu arquitectura.

---

## 🧩 Concepto clave: **Persistent Context**

Playwright normalmente abre un navegador efímero: cuando cierras la sesión se pierden cookies, sesión, etc.
Pero si usas `launch_persistent_context(user_data_dir=...)`, Playwright **crea un perfil permanente en disco**:

* `user_data_dir` = carpeta donde se guardan cookies, caché, credenciales…
* cada vez que abras Playwright con **la misma ruta**, reaparecerán **todas las cookies y la sesión**.

En otras palabras:
➡️ **Primera vez**: entras, haces login + 2FA, marcas “Recordar este dispositivo”.
➡️ **Próximas veces**: Playwright abre el navegador ya logueado, sin pedir nada.

---

## 🛠️ Instalación

En tu entorno de proyecto:

```bash
pip install playwright
playwright install chromium
```

* `playwright install chromium` descarga el binario de Chromium/Chrome que Playwright usará.

---

## 1️⃣ Sembrar la sesión (solo una vez)

La primera vez necesitamos **iniciar sesión manualmente** para que las cookies se guarden en el directorio.

### `scripts/seed_playwright_profile.py`

```python
from playwright.sync_api import sync_playwright

USER_DATA_DIR = r"C:\ChromeProfiles\mycase_pw"

with sync_playwright() as p:
    # Creamos el contexto persistente (perfil permanente)
    context = p.chromium.launch_persistent_context(
        USER_DATA_DIR,
        headless=False,          # Necesitamos ver la ventana
        channel="chrome"         # Opcional: usa Chrome en vez de Chromium
    )
    page = context.new_page()
    page.goto("https://the-mendoza-law-firm.mycase.com/dashboard")

    print("➡️  Se abrió el navegador.")
    print("👉  Inicia sesión en MyCase, completa el 2FA y marca 'Recordar este dispositivo'.")
    input("Cuando veas el dashboard y hayas terminado, pulsa ENTER aquí para cerrar.")

    context.close()
```

1. Ejecuta este script:

   ```bash
   python scripts/seed_playwright_profile.py
   ```
2. Haz login + 2FA en la ventana abierta.
3. Marca “Recordar este dispositivo”.
4. Pulsa ENTER en la consola para cerrar.

Ahora en `C:\ChromeProfiles\mycase_pw` quedan cookies y credenciales permanentes.

---

## 2️⃣ Uso diario en tu arquitectura

Cada vez que quieras scrapear, **no necesitas volver a loguearte**.

### `data_layer/playwright_driver.py`

```python
from playwright.sync_api import sync_playwright

USER_DATA_DIR = r"C:\ChromeProfiles\mycase_pw"

def create_playwright_context():
    """
    Abre el perfil persistente de Playwright (sesión ya guardada).
    Retorna: playwright, context, page
    """
    p = sync_playwright().start()
    context = p.chromium.launch_persistent_context(
        USER_DATA_DIR,
        headless=False,  # Cambia a True si quieres ocultar la ventana
        channel="chrome" # Opcional
    )
    page = context.new_page()
    return p, context, page
```

### `service_layer/mycase_play_service.py`

```python
from data_layer.playwright_driver import create_playwright_context
from config.settings import DASHBOARD_URL

def open_mycase_with_pw():
    p, context, page = create_playwright_context()
    page.goto(DASHBOARD_URL)
    print("✅ MyCase abierto con sesión persistente.")
    return p, context, page
```

### `main.py`

```python
from service_layer.mycase_play_service import open_mycase_with_pw

def main():
    p, context, page = open_mycase_with_pw()

    # Aquí ya puedes scrapear:
    # ejemplo: obtener el título de la página
    print("Título actual:", page.title())

    input("Pulsa ENTER para cerrar...")
    context.close()
    p.stop()

if __name__ == "__main__":
    main()
```

---

## 💡 Cómo se integra a tu arquitectura en capas

| Capa                     | Función                                                                                            |
| ------------------------ | -------------------------------------------------------------------------------------------------- |
| **config/**              | Guarda `USER_DATA_DIR` y `DASHBOARD_URL`.                                                          |
| **data\_layer/**         | `playwright_driver.py` → Inicializa el contexto persistente.                                       |
| **service\_layer/**      | `mycase_play_service.py` → Abre MyCase y valida la sesión.                                         |
| **parsing\_layer/**      | Aquí crearás funciones para extraer datos de las páginas Playwright (`page.query_selector`, etc.). |
| **presentation\_layer/** | Guardar CSV, DB, etc.                                                                              |
| **main.py**              | Orquesta todo.                                                                                     |

---

## ✅ Ventajas frente a Selenium/CDP

* **Sin puertos de depuración** ni conflictos de opciones.
* **Cookies permanentes** → ideal para saltar 2FA después del primer login.
* API moderna y más estable (puedes usar selectores tipo `page.locator(...)`).
* Soporta **headless** o modo visible.

---

## ⚡ Siguientes pasos

1. Haz una primera ejecución con `seed_playwright_profile.py` para guardar la sesión.
2. Usa `main.py` a diario.

   * Si algún día MyCase expira la sesión, simplemente vuelve a correr el seed.

---

## Ejemplo de scraping básico

Dentro de tu capa de parsing podrías hacer algo como:

```python
# parsing_layer/mycase_parser.py
def list_case_titles(page):
    titles = [el.inner_text() for el in page.query_selector_all(".case-title")]
    return titles
```

Y en `main.py`:

```python
from parsing_layer.mycase_parser import list_case_titles

def main():
    p, context, page = open_mycase_with_pw()
    print(list_case_titles(page))
    input("ENTER para cerrar...")
    context.close()
    p.stop()
```

---

### 🗝️ En resumen

* **Primera vez**: ejecuta `seed_playwright_profile.py`, haz login + 2FA.
* **Después**: cada scraping abre **el mismo perfil persistente**, y MyCase ya estará logueado.
* Playwright se encarga de mantener cookies/tokens sin que tengas que revalidar.
